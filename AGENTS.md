# Su OS Agent Instructions

本文件适用于整个仓库。它定义 Codex 的工作方式；产品说明以 `README.md` 为准，稳定架构与数据流以 `ARCHITECTURE.md` 为准。代码与文档不一致时，先核实真实行为并在交付报告中指出，不要自行猜测。

## 开始工作

- 修改前阅读与任务相关的 `README.md`、`ARCHITECTURE.md`、现有实现和测试/脚本。
- 先检查 `git status` 与相关 diff，保留用户的未提交改动，不覆盖或顺手整理无关文件。
- 用户要求修改单个模块时，不得顺带重构其他模块。
- 优先做最小、可验证、可回退的改动；不要创建 `*-v2`、`*-new`、`*-final` 等平行版本文件。
- 未经明确需要，不新增依赖、不升级主版本、不调整目录边界、不更换状态或持久化方案。

## 架构边界

- `src/app` 只负责路由、全局样式和 Provider 装配；页面文件保持轻量。
- 业务 UI、hooks 和 selectors 放在对应的 `src/modules/<feature>`；跨模块只使用公开类型、hook 或纯 selector。
- `src/domain` 必须是纯 TypeScript，不依赖 React、浏览器 API、存储实现、Supabase SDK 或数据库行类型。
- `src/data` 负责持久化、备份、导入校验和映射；业务组件不得直接访问 `localStorage`。
- `src/store` 统一协调业务变更与持久化。连续更新应使用函数式状态更新，并在同一入口维护业务不变量。
- selector 保持纯函数、可独立测试，不执行 I/O，也不修改输入。
- `src/shared` 只放真正跨模块的 UI、布局、配置和工具。布局需要业务数据时，应调用模块公开 API，避免在 Shared 中重写业务统计。
- Dashboard 可以组合多个模块的公开 selector，但模块之间不要导入彼此的内部组件。

## 数据与兼容性

- 未经明确迁移方案，不修改 `su-os:data:v2`、`schemaVersion: 2` 或当前快照结构。
- 区分“首次无数据”“数据损坏”“读取失败”和“写入失败”；不得以演示数据静默覆盖无法解析的原始数据。
- 导入数据必须先校验结构、日期/时间、取值范围、唯一 ID、实体引用和跨字段不变量，再整体替换。
- 修改项目、任务、专注、笔记或设置模型时，同时检查 Store、repository、导入导出、demo 数据和 selectors 的兼容性。
- `FocusDraft` 是设备临时状态，不默认进入云同步；持久业务实体与临时 UI 状态保持分离。
- 破坏性数据操作必须有确认，并优先提供导出/恢复路径。

## 云端扩展边界

- 未来接入 Supabase 时使用异步、表级 CRUD 与显式 Mapper，不把整个本地快照当作云端单一 JSON blob。
- Domain 不感知 Supabase；UI 不直接调用 Supabase SDK。
- 云端实现必须同时考虑 `user_id`、RLS、认证、离线状态、错误恢复、冲突策略和迁移。
- 不为尚未实现的云端能力提前污染当前同步接口；需要时新增清晰的异步接口并保留本地适配器。

## UI 与移动端

- 延续现有 Liquid Glass 视觉和 CSS 变量/工具类，优先复用现有组件。
- 每个 UI 改动同时检查桌面端和手机端，包括 safe area、触控尺寸、滚动、软键盘和横竖屏窄宽度。
- 关键导航在 hydration 前后都必须可用；Local-first 加载应有明确的 loading、empty 和 error 状态。
- 可交互元素必须有真实行为、语义标签、键盘操作和清晰焦点；弹层需考虑焦点恢复、Escape 和背景滚动。
- 动画遵守 `prefers-reduced-motion`。未经要求不要引入 WebGL、重型动画库或新的设计系统。

## 安全与依赖

- 不提交密钥、令牌、真实凭据、私有地址或个人数据。环境变量只写入本地 `.env*`，公共示例只放占位值。
- 不记录敏感数据，不扩大导入/导出的数据范围，不引入不必要的网络请求或遥测。
- 依赖变更必须有任务依据，并同步更新 `package.json` 与 lockfile；提交前检查安装脚本、安全公告和主版本风险。
- 保持 CI 最小权限。修改工作流时固定关键行为、设置合理超时，并避免向不可信上下文暴露 secrets。
- 涉及 Next.js、React、Tailwind CSS、Vitest、Supabase、Capacitor 等第三方库的 API、配置、版本行为或升级时，优先通过 Context7 查询当前版本文档和官方用法，不要仅凭模型记忆实现；查询结果必须结合本仓库当前实际版本和代码判断，不能为了匹配文档擅自升级依赖。

## 验证与交付

- 源码、配置或依赖变化后运行 `npm run lint` 和 `npm run build`。
- 行为修复需手动验证相关路由和交互；涉及响应式 UI 时至少检查一个桌面宽度和一个手机宽度。
- 修改纯 selector、导入校验、Store 业务规则时，优先补回归测试；仓库存在测试命令后必须运行相关测试。
- 仅文档变更至少运行 `git diff --check` 并核对链接、命令、版本和文件名。
- 最终说明改了什么、验证结果、未解决风险；不要把未运行的检查描述为已通过。

## Git 与生成文件

- 只有用户明确要求时才 commit、push 或创建 PR；提交前只暂存本任务文件并复查 staged diff。
- 不 force-push、不重写历史、不删除用户改动，除非用户明确授权。
- 不提交 `.env*`、`.next/`、日志、临时文件或本地备忘。`LOCAL_NOTES.md` 是被忽略的私人日志，不是远程状态的可信来源。
- `next-env.d.ts` 由 Next.js 生成，不手工编辑，也不提交仅由 dev/build 模式切换产生的路径抖动。
- 根目录本文件是项目级 Codex 指令源；`next.config.ts` 中的 `agentRules: false` 用于避免 Next.js 自动生成重复规则，不要随意改动。
